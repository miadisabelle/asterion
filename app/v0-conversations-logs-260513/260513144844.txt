The user is on mobile (iPhone/iOS) and the navigation is not optimal. I need to improve the mobile experience across the app. Let me:

First check the current sidebar and layout components to understand what we have
Look at what mobile-related hooks/components exist in the project
Implement mobile-friendly navigation patterns:

Collapsible sidebar/drawer for mobile
Better touch targets
Responsive layouts
Mobile-first navigation



Let me start by examining the current state of the codebase.Explore • 3 Files • 1 SearchExplore • 4 FilesGood, I see the existing patterns. The project has `useIsMobile` hook and `Sheet` component. The main app sidebar is fixed on the left and never collapses on mobile. The docs layout hides the sidebar on mobile but has no way to access it. Let me check the main page to understand the full layout:

Explore • 2 FilesI see the pattern - every page imports `Sidebar` and uses `pl-64` (padding-left for the fixed sidebar width). This breaks on mobile since the sidebar is always visible and the main content is pushed 256px to the right. I need to:

1. Create a mobile-responsive wrapper with a collapsible sidebar using Sheet
2. Add viewport meta for iOS Safari
3. Update the main Sidebar component to work with mobile drawer
4. Create a shared layout component to reduce duplication


Let me build these improvements:

Created app shellNow let me update the layout to add the iOS viewport meta and then update the pages to use the new AppShell:

Read layoutAdded Viewport importAdded viewport configNow let me update the main pages to use the AppShell. Starting with the dashboard:

Updated dashboard pageUpdated tensions pageNow let me update the remaining pages. I'll batch them for efficiency:

Explore • 4 FilesNow I'll update all these pages to use AppShell:

Updated threads pageNow let me update the graph page and the remaining detail pages:

Explore • 3 FilesThese are longer files. Let me update them to use AppShell as well:

Updated graph pageUpdated project detailNow let me update the docs layout for mobile support as well:

Updated docs layoutNow I need to update the DocSidebar to accept an onNavigate callback:

Read doc sidebarAdded onNavigate propUpdated function signaturePassed onNavigate to NavItemAdded onNavigate to NavItemPropsUpdated NavItem signatureAdded onClick and touch targetPassed onNavigate to childrenNow let me also remove the old Sidebar component import from any files that might still reference it (though we've already updated all the pages). Let me clean up the old standalone Sidebar component to add a deprecation note:

Read old sidebarThe old Sidebar is no longer used since all pages now use AppShell. I can delete it:

Deleted old sidebarv3Restore
The mobile navigation improvements are complete. Here's what was done:

**Core Changes:**
- Created `AppShell` component (`components/asterion/app-shell.tsx`) - a responsive layout wrapper that handles desktop sidebar and mobile drawer navigation using the Sheet component
- Added viewport metadata to `layout.tsx` with `maximumScale: 1` and `userScalable: false` to prevent iOS Safari zoom on input focus
- Added theme-color meta for proper iOS Safari chrome appearance

**Mobile UX Improvements:**
- All navigation links now have 44px minimum touch targets (iOS standard)
- Sidebar converts to a slide-out drawer on mobile screens (<768px)
- Header bar with hamburger menu button for mobile navigation
- Dialog content is scrollable with `max-h-[90vh]` to prevent overflow
- Form inputs use `text-base` class (16px) to prevent iOS zoom on focus
- Responsive grid layouts change from 3 columns to 2 to 1 on smaller screens
- Truncation with `truncate` and `max-w-*` for long text on mobile
- Docs layout also updated with mobile-friendly sidebar drawer